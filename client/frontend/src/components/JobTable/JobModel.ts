
// Define the type for each job object
export interface Job {
    id?: string; // Make the id property optional
    emploi: string;
    lien: string;
    date: string;
    ville: string;
    site: string;
    versionCV: string;
    status: string;
  }